import styled from 'styled-components';

const CreateNaeggeotalkTemplate = () => {
  return <Wrap></Wrap>;
};

const Wrap = styled.div`
  width: 90%;

  margin: 0 auto;
  margin-top: 20px;
`;

export default CreateNaeggeotalkTemplate;
